"""Study-oriented grouping for backend data access helpers.

These modules re-export functions from ``models.py`` so the codebase is easier
to read by domain without changing the current runtime imports.
"""
